// The content of this file is inlined in the Emscripten js output and can be
// used to initialize the global Module which is the primary Emscripten
// environment.
// NOTE: cannot use import because it upsets Emscripten build pipeline
Module = require('../../web/src/module').Module;